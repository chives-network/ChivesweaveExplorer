import FileResources from 'src/views/model/myfiles';

const FileResourcesList = () => {

  const activeTab = "excel"; 

  return <FileResources activeTab={activeTab} />

}

export default FileResourcesList

